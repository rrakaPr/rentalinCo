# Railway Nixpacks configuration for PHP
[phases.setup]
nixPkgs = ["php83", "php83Extensions.pdo", "php83Extensions.pdo_mysql", "php83Extensions.mbstring", "php83Extensions.json"]

[phases.install]
cmds = ["mkdir -p /var/log/nginx"]

[start]
cmd = "php -S 0.0.0.0:${PORT:-8080} -t ."
